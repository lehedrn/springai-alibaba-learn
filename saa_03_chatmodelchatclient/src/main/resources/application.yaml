server:
  port: 18080
  servlet:
    encoding:
      enabled: true
      force: true
      charset: UTF-8

spring:
  application:
    name: saa_03_chatmodelchatclient
  ai:
    dashscope:
      api-key: ${DASHSCOPE_API}